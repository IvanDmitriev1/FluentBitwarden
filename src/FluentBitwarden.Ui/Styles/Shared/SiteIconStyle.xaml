<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sharedTemplates="using:FluentBitwarden.Templates.Shared"
    xmlns:siteIcon="using:FluentBitwarden.Controls.Shared.SiteIcon">

    <ResourceDictionary.MergedDictionaries>
        <sharedTemplates:SiteIconTemplates />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="siteIcon:SiteIcon">
        <Setter Property="Size" Value="28" />
        <Setter Property="FallbackGlyph" Value="&#xE774;" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
        <Setter Property="ContentTemplateSelector" Value="{StaticResource SiteIconTemplateSelector}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="siteIcon:SiteIcon">
                    <ContentControl
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
